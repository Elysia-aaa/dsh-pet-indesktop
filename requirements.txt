PySide6>=6.5
imageio-ffmpeg>=0.6
Pillow>=10
keyring>=25
certifi>=2024.0.0
pycaw>=0.0.13; platform_system == "Windows"
tzdata>=2024.1

# —— 歌词显示（可选）——
# 「显示歌词」功能靠 Windows 系统媒体控制（SMTC）读取当前播放的曲目。
# 不装不影响桌宠本体：now_playing.available() 会返回 False，
# 设置页的歌词开关置灰并说明原因，其余功能一切照旧。
# 打包时注意 winrt 是 namespace package，需在 hiddenimports 里登记子模块
# （winrt.windows.media.control / winrt.windows.foundation.collections），
# 否则 PyInstaller 会漏收。
winrt-Windows.Media.Control>=3.0; platform_system == "Windows"
winrt-Windows.Foundation>=3.0; platform_system == "Windows"
winrt-Windows.Foundation.Collections>=3.0; platform_system == "Windows"
winrt-Windows.Storage.Streams>=3.0; platform_system == "Windows"

# —— 开发依赖（测试 + 静态检查）——
# pytest：测试套件入口（README「测试与验证」与本仓库 CI 都依赖它）。
# ruff：`ruff check pet/ tests/` 用的静态检查器（规则基线见 pyproject.toml
# 的 [tool.ruff]，仅 F 级）。不参与运行时；声明在此使 fresh environment
# 能按 requirements 复现静态检查（FINAL_REVIEW_GPT.md 提交卫生项，批10 补）。
pytest>=8
ruff>=0.5
